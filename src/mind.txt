config{
	categorymode :	// 分类模式（ 0:快捷方式/副本开放分类模式 | 1:文件格式开放分类模式 | 2:非开放分类模式 ）
	blogIndex		// 目录名 / 日期 / 文章名
}

input{
	src:			//（源数据目录）
	dest:			//（生成目录）
	genMode:		//（生成模式：仅目录|仅文章|全部重建）
}

程序：

foreach src{

	blog_dir,blog_date

	$blogInfo = parseBlog(blog_dir);

	if(gen == 1 || genMode == 2){

	
	}

}

if(genMode == 0 || genMode == 2){

	//生成索引
	makeIndex()

} 



//解析博客
function parseBlog(){

	if(全部是图片){
	
		decodePhoto()
	
	}else{

		decodeArticle()
	
	}
	

}

//解析文章型博客
function decodeArticle(){
	
	if(全是目录){

		foreach{

			$photoInfo = 读取图片exif信息;

		}

	}

	return articleInfo
	
}

//生成文章型博客
function makeArticle(){
	
	createHTML();

	copy();

}

//解析相册型博客
function decodePhoto(){
	
	foreach{

		$photoInfo = 读取图片exif信息;

	}

	return photoInfo
	
}

//生成相册型博客
function makePhoto(){
	
	createHTML();

	copy();

}

